# Eyelingo desktop — zależności runtime (fiszki_app.py)
PyQt6==6.7.1
# >= 2.16 wymagane dla kluczy `sb_publishable_...` (starsze walidowaly klucz jako JWT).
# Zweryfikowane na 2.31.0: ClientOptions(auto_refresh_token=False), auth.*, postgrest.auth(),
# table(), rpc() — pelne API uzywane przez fiszki_app.py dziala bez zmian.
supabase==2.31.0
python-dotenv==1.0.1
gTTS==2.5.4
# Windows-only: globalne skróty klawiszowe. Na macOS wymagają roota — kod ich tam nie ładuje.
keyboard==0.13.5 ; sys_platform == "win32"
# pyobjc: polityka „accessory" (brak ikony w Docku) + click-through nakładki na macOS
pyobjc-framework-Cocoa==10.3.1 ; sys_platform == "darwin"
